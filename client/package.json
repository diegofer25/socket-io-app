{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clear && npm i && parcel build index.html",
    "clear": "npm run clear:modules && npm run clear:dist",
    "clear:modules": "rm -rf node_modules",
    "clear:dist": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/socket.io-client": "^1.4.34",
    "parcel": "^1.12.4"
  },
  "dependencies": {
    "socket.io-client": "^3.0.0"
  }
}
